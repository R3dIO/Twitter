// let MentionID = MentionDataTable.Columns.Add("MentionID", typeof<int32>);
// MentionID.AutoIncrement = true;
// MentionID.AutoIncrementSeed = 1L;
// MentionID.AutoIncrementStep = 1L;

// let updateTempRow = HashTagDataTable.Select("HashTag='" + hashtag.Value + "'")
// updateTempRow.[0].["TweetID"] <- updateTempRow.[0].["TweetID"] + ";" + tweetID

// let updateTempRow = MentionDataTable.Select("Mention='" + mention + "'")
// updateTempRow.["TweetID"] <- updateTempRow.["TweetID"] + ";" + tweetID

System.Threading.Thread.Sleep(1000)

// TweetIDCol.AutoIncrement = true;
// TweetIDCol.AutoIncrementSeed = 1L;
// TweetIDCol.AutoIncrementStep = 1L;